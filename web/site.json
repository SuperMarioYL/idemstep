{
  "schema": 3,
  "name": "IdemStep",
  "github": "https://github.com/SuperMarioYL/idemstep",
  "host": "idemstep.lei6393.com",
  "palette": "auto",
  "lang": {
    "primary": "en",
    "toggle": true
  },
  "meta": {
    "visual_style": "neon-particle",
    "demo_source": "docs/demo-results.json"
  },
  "hero": {
    "headline": {
      "en": "Make a retry reuse its first result",
      "zh": "让重试复用第一次结果"
    },
    "sub": {
      "en": "IdemStep associates an action with a stable key. Its JavaScript wrapper reuses completed step results, and its HTTP proxy replays cached responses for repeated keyed requests.",
      "zh": "IdemStep 为操作绑定稳定的 Key。JavaScript 包装器复用已完成步骤的结果，HTTP 代理则为重复的带 Key 请求回放缓存响应。"
    },
    "href": "#quickstart"
  },
  "advantages": [
    {
      "title": {
        "en": "Reuse a result",
        "zh": "复用结果"
      },
      "body": {
        "en": "Completed keys replay cached wrapper values or HTTP responses.",
        "zh": "已完成的 Key 回放包装器返回值或 HTTP 响应。"
      }
    },
    {
      "title": {
        "en": "Coalesce concurrent retries",
        "zh": "合并并发重试"
      },
      "body": {
        "en": "Same-key callers share an in-flight operation within the process.",
        "zh": "同一进程内，同 Key 调用共享进行中的操作。"
      }
    },
    {
      "title": {
        "en": "Choose the retention window",
        "zh": "选择保留窗口"
      },
      "body": {
        "en": "Use an in-memory store or JSON persistence, with optional committed-key TTL.",
        "zh": "可使用内存或 JSON 持久化存储，并设置已完成 Key 的 TTL。"
      }
    }
  ],
  "demo": {
    "title": {
      "en": "Run it on a complete example",
      "zh": "运行完整示例"
    },
    "note": {
      "en": "The script starts a real local proxy and a synthetic order endpoint. Two POSTs share order-demo-1; the third uses order-demo-2. It counts actual upstream calls and reads the replay header.",
      "zh": "脚本启动实际本地代理和合成订单接口。两次 POST 使用 order-demo-1，第三次使用 order-demo-2，统计实际上游调用并读取回放 Header。"
    },
    "source": "https://github.com/SuperMarioYL/idemstep/blob/main/docs/demo-results.json",
    "steps": [
      {
        "label": "Recorded run",
        "body": "Real HTTP proxy deduplication against a loopback fixture; no browser or external transaction.",
        "command": "node examples/presentation-demo.mjs",
        "output": "{\"key\":\"order-demo-1\",\"status\":200,\"replayed\":false,\"body\":{\"order\":1}}\n{\"key\":\"order-demo-1\",\"status\":200,\"replayed\":true,\"body\":{\"order\":1}}\n{\"key\":\"order-demo-2\",\"status\":200,\"replayed\":false,\"body\":{\"order\":2}}\nupstream calls=2; suppressed retries=1\nScope: local HTTP fixture; no browser, payment provider or external order.",
        "image": "/assets/presentation/demo-0-light.svg",
        "imageDark": "/assets/presentation/demo-0-dark.svg"
      }
    ]
  },
  "quickstart": {
    "title": {
      "en": "Try it locally",
      "zh": "本地上手"
    },
    "body": {
      "en": "The script starts a real local proxy and a synthetic order endpoint. Two POSTs share order-demo-1; the third uses order-demo-2. It counts actual upstream calls and reads the replay header.",
      "zh": "脚本启动实际本地代理和合成订单接口。两次 POST 使用 order-demo-1，第三次使用 order-demo-2，统计实际上游调用并读取回放 Header。"
    },
    "steps": [
      {
        "title": {
          "en": "Install from source",
          "zh": "从源码安装"
        },
        "code": "git clone https://github.com/SuperMarioYL/idemstep.git\ncd idemstep\nnpm ci\nnpm run build"
      },
      {
        "title": {
          "en": "Run the included example",
          "zh": "运行随仓示例"
        },
        "code": "node examples/presentation-demo.mjs"
      }
    ]
  },
  "limitations": [
    {
      "en": "This is local deduplication, not an end-to-end exactly-once guarantee. A crash after an upstream side effect but before durable commit can leave an ambiguous outcome.",
      "zh": "这是本地去重，不能保证端到端严格执行一次。上游已生效但尚未持久化提交时崩溃，结果仍可能不确定。"
    },
    {
      "en": "The store is not designed for independently running processes to concurrently share one JSON file.",
      "zh": "存储不适用于多个独立进程并发共享同一 JSON 文件。"
    },
    {
      "en": "The demo covers local HTTP responses; it does not validate browser integration, payment APIs or HTTPS trust setup.",
      "zh": "示例覆盖本地 HTTP 响应，没有验证浏览器集成、支付 API 或 HTTPS 信任配置。"
    }
  ],
  "scene": {
    "kind": "neon-graph",
    "fallback": "/assets/presentation/scene-dark.svg",
    "fallbackLight": "/assets/presentation/scene-light.svg"
  },
  "story": {
    "intro": {
      "title": {
        "en": "A concrete task",
        "zh": "从实际任务出发"
      },
      "body": {
        "en": "A slow response can make a browser workflow retry a submit that already completed. Keeping one key for that logical action lets the local store recognize the retry, while a new key explicitly starts a new action.",
        "zh": "响应缓慢可能使浏览器工作流再次提交已经完成的操作。同一逻辑操作沿用一个 Key，本地存储即可识别重试；新 Key 则明确表示新操作。"
      }
    },
    "sections": [
      {
        "title": {
          "en": "Follow the operation",
          "zh": "处理过程"
        },
        "body": {
          "en": "idemStep owns the local function lifecycle. IdemStore tracks pending and committed records plus cached values. startProxy reads x-idem-key, forwards the first request, stores the full response and serves later same-key calls from that cache. In-flight maps coalesce concurrent callers; JSON persistence keeps completed records for a restart.",
          "zh": "idemStep 管理本地函数生命周期；IdemStore 保存 pending、committed 状态及缓存值。startProxy 读取 x-idem-key，转发首次请求，存储完整响应，再从缓存处理后续同 Key 调用。进行中映射合并并发调用，JSON 持久化保留已完成记录供重启使用。"
        },
        "image": {
          "light": "/assets/presentation/process-light.svg",
          "dark": "/assets/presentation/process-dark.svg",
          "alt": {
            "en": "Follow the operation",
            "zh": "处理过程"
          }
        }
      },
      {
        "title": {
          "en": "One key joins the step, store and proxy",
          "zh": "Key 串联步骤、存储与代理"
        },
        "body": {
          "en": "idemStep owns the local function lifecycle. IdemStore tracks pending and committed records plus cached values. startProxy reads x-idem-key, forwards the first request, stores the full response and serves later same-key calls from that cache. In-flight maps coalesce concurrent callers; JSON persistence keeps completed records for a restart.",
          "zh": "idemStep 管理本地函数生命周期；IdemStore 保存 pending、committed 状态及缓存值。startProxy 读取 x-idem-key，转发首次请求，存储完整响应，再从缓存处理后续同 Key 调用。进行中映射合并并发调用，JSON 持久化保留已完成记录供重启使用。"
        },
        "image": {
          "light": "/assets/presentation/architecture-light.svg",
          "dark": "/assets/presentation/architecture-dark.svg",
          "alt": {
            "en": "One key joins the step, store and proxy",
            "zh": "Key 串联步骤、存储与代理"
          }
        }
      },
      {
        "title": {
          "en": "Connect the pieces",
          "zh": "输入与输出"
        },
        "body": {
          "en": "The wrapper and proxy are separate integration points. The wrapper does not automatically attach network headers to a browser request: configure the browser’s proxy and request headers explicitly. HTTP requests without an idempotency key pass through without deduplication.",
          "zh": "包装器和代理是两个接入位置。包装器不会自动向浏览器请求添加网络 Header，需显式配置浏览器代理及请求头。不带幂等 Key 的 HTTP 请求透传，不参与去重。"
        },
        "image": {
          "light": "/assets/presentation/integrations-light.svg",
          "dark": "/assets/presentation/integrations-dark.svg",
          "alt": {
            "en": "Connect the pieces",
            "zh": "输入与输出"
          }
        }
      }
    ],
    "details": [
      {
        "title": {
          "en": "Configuration",
          "zh": "配置"
        },
        "body": {
          "en": "--host 127.0.0.1 keeps the proxy local; omitted host binds all interfaces. --store enables JSON persistence. --ttl expires committed keys, after which the same key may forward again; --prune-interval sweeps expired records. --upstream-timeout bounds idle upstream traffic. --https requires OpenSSL and client trust in the generated CA. Hosted mode adds configurable API-key namespaces but does not provide a distributed transaction store or billing service.",
          "zh": "--host 127.0.0.1 将代理限制在本机，省略 host 会监听所有接口。--store 启用 JSON 持久化。--ttl 使已完成 Key 过期，之后同 Key 可能再次转发；--prune-interval 清理过期记录。--upstream-timeout 限定上游空闲时间。--https 需要 OpenSSL，并要求客户端信任生成的 CA。hosted 模式提供可配置 API Key 命名空间，但没有分布式事务存储或计费服务。"
        }
      },
      {
        "title": {
          "en": "Further use",
          "zh": "进一步使用"
        },
        "body": {
          "en": "For the library, import idemStep, generateKey and IdemStore from idemstep. Generate the key once for a logical operation, keep it across retries, and pass the same store to calls that share results. For the proxy, send x-idem-key with the intercepted request; see examples/presentation-demo.mjs for complete HTTP input. A new intent needs a new key even if its body is identical.",
          "zh": "库用法从 idemstep 导入 idemStep、generateKey 和 IdemStore。每项逻辑操作生成一次 Key，重试时保留，需共享结果的调用使用同一存储。代理用法是在被拦截请求中传入 x-idem-key，完整 HTTP 输入见 examples/presentation-demo.mjs。新的操作意图即使内容相同，也应使用新 Key。"
        },
        "code": "node dist/index.js proxy --host 127.0.0.1 --port 8473 --store ./idemstep-state.json --ttl 86400000 --prune-interval 60000"
      }
    ]
  },
  "content_version": "v0.11.0"
}
